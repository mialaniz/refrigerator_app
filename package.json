{
  "name": "refrigerator_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "concurrently \"cd backend && node index.js\" \"cd frontend && npm run dev\"",
    "install": "concurrently \"cd backend && npm i\" \"cd frontend && npm i\"",
    "prepare": "npm run install",
    "vercel-build": "npm run prepare && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
